# Образ = код + зависимости. Корпус, ingest, чанки — не в поставку (только runtime / volume при необходимости).
.venv
.git
__pycache__
*.pyc
data/
archive/
# Крупные локальные реестры не тащим в образ; при профиле docling передайте свой файл через volume или смонтируйте каталог.
inventory*.xlsx